/* global describe, test, expect */
import lib from './index';

describe('frint-intl-react :: index', function () {
  test('is an object', function () {
    expect(typeof lib).toBe('object');
  });
});
